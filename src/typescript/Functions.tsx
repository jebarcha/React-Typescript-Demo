
const sum = ( a: number, b: number ): number => {
    return a + b;
}

export const Functions = () => {
    return (
        <>
            <h3>Functions</h3>  
            <span>Result is: { sum(1, 2) }</span>
        </>
    )
}
